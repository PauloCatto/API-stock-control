{
  "name": "stock-api",
  "version": "1.0.0",
  "main": "src/server.ts",
  "license": "MIT",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "start": "ts-node src/server.ts",
    "dev": "ts-node-dev --transpile-only src/server.ts",
    "build": "npx prisma generate --schema=prisma/schema.prisma && npx prisma migrate deploy --schema=prisma/schema.prisma && tsc",
    "postinstall": "npx prisma generate --schema=prisma/schema.prisma"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.2",
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/jsonwebtoken": "^9.0.1",
    "prisma": "^6.7.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.1.0"
  },
  "dependencies": {
    "@prisma/client": "^6.7.0",
    "bcryptjs": "^2.4.3",
    "connect-timeout": "^1.9.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^9.0.0",
    "serverless-http": "^3.2.0"
  }
}
